source 'https://rubygems.org/'

gem 'irkit'
gem 'pry'
gem 'whenever', require: false

group :deploy do
  gem 'serverkit'
  gem 'serverkit-rbenv'

  # support ssh-ed25519
  gem 'rbnacl', '>= 3.2', '< 5.0'
  gem 'rbnacl-libsodium'
  gem 'bcrypt_pbkdf', '>= 1.0', '< 2.0'
end
