Yamamayaa
=========

```
$ git clone https://github.com/hanachin/yamamayaa.git
$ cd yamamayaa
$ bundle check --path=.bundle || bundle install --path=.bundle
$ bin/serverkit apply recipe.yml --hosts=yamamayaa.local
```
